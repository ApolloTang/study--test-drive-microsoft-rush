install typescript
get typecheck to work
migrate font**.js to font**.tsx?
add webpack path alias

move fontcheck to external module (share)
move font to external module (share)
throughtly test typescript ide functionality

todo items below is done:
`````````````````````````
Get frontloading to work
build a ui component that can handle images


